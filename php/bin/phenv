#!/usr/bin/env bash

abort() {
    (>&2 printf "\033[0;31m$1\033[0m\n")
    exit 1
}

command="$1"
shift 1

command_path="$(command -v "phenv-$command" || true)"

[ -n "$command_path" ] || abort "no such command \`$command'"

exec "$command_path" "$@"
