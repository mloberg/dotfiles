#!/usr/bin/env bash

if [ -n "$PHP_VERSION" ]; then
    echo "$PHP_VERSION"
elif [ -f ".php-version" ]; then
    cat .php-version
elif [ -f "$HOME/.php-version" ]; then
    cat ~/.php-version
else
    echo "system"
fi
